name: Showtimes CI

on:
  push:
    paths:
      - 'CNM.Showtimes.API/**'
  pull_request:
    paths:
      - 'CNM.Showtimes.API/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '3.1.x'
      - name: Restore
        run: dotnet restore CNM.Showtimes.API/CNM.Showtimes.API.csproj
      - name: Build
        run: dotnet build CNM.Showtimes.API/CNM.Showtimes.API.csproj --configuration Release --no-restore
      - name: Publish
        run: dotnet publish CNM.Showtimes.API/CNM.Showtimes.API.csproj --configuration Release --no-build -o out
      - name: Build Docker image
        run: docker build -t cnm-showtimes-api:latest CNM.Showtimes.API
